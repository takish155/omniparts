import React from "react";
import { Skeleton } from "@/components/ui/skeleton";

const ProductSkeleton = () => {
  return <Skeleton className="min-w-[280px] h-[400px] mb-8" />;
};

export default ProductSkeleton;
