import React from "react";
import { Skeleton } from "@/components/ui/skeleton";

const ProductSkeleton = () => {
  return <Skeleton className="min-w-[280px] h-[50vh] mb-8" />;
};

export default ProductSkeleton;
